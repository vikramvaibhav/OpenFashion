import React, { memo } from 'react'
import { Text, View } from 'react-native'

const CollectionScreen = () => {
    return (
        <View>
            <Text>CollectionScreen</Text>
        </View>
    )
}

export default memo(CollectionScreen)